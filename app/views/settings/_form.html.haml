= semantic_form_for @setting, :html => { :class => 'form-horizontal' } do |f|
  = f.inputs do
    = f.input :var
    = f.input :value, :as => :string
  = f.actions do
    = f.action :submit, :label => 'Save'
    = f.action :cancel, :label => 'Cancel'
